language: c
sudo: required
dist: trusty
script:
 - cd ../.. && make defconfig && make


branches:
  only:
    - development

before_install:
  - sudo apt-get -qq update
  - git clone git://git.openwrt.org/14.07/openwrt.git barrier-breaker
  - cd barrier-breaker
  - ./scripts/feeds update -a
  - cd package

  
addons:
  apt:
    packages:
    - libasound2-dev
    - libsoxr-dev
    - libpopt-dev
    - libssl-dev
    - libpolarssl-dev
    - libconfig-dev
    - libncurses5-dev
    - unzip
    - subversion
    - zlib1g-dev
    - zlib-static
    - gettext

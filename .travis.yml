language: c
sudo: required
dist: trusty
script:
 - cd ~/barrier-breaker
 - make defconfig
 - make


branches:
  only:
    - barrier-breaker

before_install:
  - git clone git://git.openwrt.org/14.07/openwrt.git barrier-breaker
  - cd barrier-breaker
  - ./scripts/feeds update -a
  - cd package

  
addons:
  apt:
    packages:
    - libssl-dev
    - libncurses5-dev
    - unzip
    - subversion
    - zlib1g-dev
    - gawk
    - gcc-multilib
    - flex
    - gettext
